1、代码背景：
   some/ip协议中的event是实时上报的，以1us的频率。目前我们的工作是，解析实时上报的数据。（通过上位机触发解析）
    所以很明显的第一个问题是：
    1、如何存储实时上报的数据？
    2、如何保证上位机正在使用数据的时候，数据头指针不被覆盖？
    3、如何保证尽量少的内存释放与申请？
    
    
    
因此设计如下：
      1、设计一个队列，此队列只有两个元素。
      2、在插入元素时，判断元素的flag，如果不为0,则不对头元素进行处理，仅需要更新尾指针的数据即可。如果为0,则先更新头指针的数据，然后头指针和尾指针调换，即可。
      
